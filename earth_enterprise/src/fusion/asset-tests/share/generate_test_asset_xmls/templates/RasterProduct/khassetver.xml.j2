<?xml version="1.0" encoding="UTF-8" standalone="no" ?>
<RasterProductAssetVersion>

  <name>{{ name }}</name>

  <type>Imagery</type>

  <subtype>Product</subtype>

  <version>{{ version }}</version>

  <state>{{ state }}</state>

  <progress>0</progress>

  <locked>0</locked>

  <inputs>
{% for name in inputs %}
    <input>{{ name }}</input>
{% endfor %}
  </inputs>

  <children>
{% for name in children %}
    <children_element>{{ name }}</children_element>
{% endfor %}
  </children>

  <parents/>

  <listeners>
{% for name in listeners %}
    <listener>{{ name }}</listener>
{% endfor %}
  </listeners>

  <outfiles/>

  <meta>
    <item key="createdtime">2018-07-26 01:25:06</item>
    <item key="extents">90,-90,180,-180</item>
    <item key="maxlevel">14</item>
    <item key="minlevel">8</item>
    <item key="preview">{{ asset_name }}/product.kia/ver001/preview.png</item>
    <item key="sourcedate">0000-00-00T00:00:00Z</item>
  </meta>

  <beginTime>0</beginTime>

  <progressTime>0</progressTime>

  <endTime>0</endTime>

  <taskid>0</taskid>

  <config>
    <fill></fill>
    <srs></srs>
    <clampNonnegative>0</clampNonnegative>
    <maskgenConfig>
      <feather>100</feather>
      <mode>Mask</mode>
      <band>1</band>
      <fillvalue>-1</fillvalue>
      <threshold>0</threshold>
      <holesize>0</holesize>
      <whitefill>0</whitefill>
      <nodata></nodata>
    </maskgenConfig>
    <fuid_resource>1000</fuid_resource>
  </config>

</RasterProductAssetVersion>
